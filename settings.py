# Use this directory by default to place the new created files
# Using this, you will only need to provide the relative folder name
# NOT the full path. Omit this to pass a full path.
CREATE_DIRECTORY = None